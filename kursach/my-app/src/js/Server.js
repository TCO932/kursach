class Server {
    /*function getTable(props) {
  let content = {};
  fetch(`/api/${props.tableName}`)
    .then(answer => {
      answer.json().then(answer => {
        content = (answer && answer["result"] === "ok") ? answer["content"] : false;
      })
    })
  return <div id="getTable">
    Table
    {content}
  </div> 
  function showTable(tableName) {
  fetch(`/api/${tableName}`)
    .then(request => {/*
      request.json().then(answer => {
        let content = answer && answer["result"] === "ok" ? answer["content"] : [];
        setContent(content);
      })
      const content = request && request["result"] === "ok" ? request["content"] : "empty";
      //setContent(content);
      render(
        <Table table={content}/>,
        document.getElementById("workSpace")
      );
    });
}
  */
}

